#!/bin/sh
set -eu

# Check every destination ref, including HEAD:main and branch deletion.
while read -r local_ref local_oid remote_ref remote_oid; do
  if [ "$remote_ref" = refs/heads/main ]; then
    echo "Push blocked: main is protected. Push the task branch and use a pull request." >&2
    exit 1
  fi
done
